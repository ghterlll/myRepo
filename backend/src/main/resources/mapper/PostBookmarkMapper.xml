<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mobile.aura.mapper.PostBookmarkMapper">

    <!-- Result Map -->
    <resultMap id="BaseResultMap" type="com.mobile.aura.domain.content.PostBookmark">
        <result column="post_id" property="postId"/>
        <result column="user_id" property="userId"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <!-- Count bookmarks for a user on a post -->
    <select id="countBookmark" resultType="long">
        SELECT COUNT(1) FROM post_bookmark
        WHERE user_id = #{userId} AND post_id = #{postId}
    </select>

    <!-- Delete bookmark by user and post -->
    <delete id="deleteBookmark">
        DELETE FROM post_bookmark
        WHERE user_id = #{userId} AND post_id = #{postId}
    </delete>

    <!-- List bookmarked posts by user with cursor-based pagination -->
    <select id="listByUser" resultMap="BaseResultMap">
        SELECT post_id, user_id, created_at
        FROM post_bookmark
        WHERE user_id = #{userId}
        <if test="cursorTimestamp != null">
            AND (created_at &lt; #{cursorTimestamp}
            OR (created_at = #{cursorTimestamp} AND post_id &lt; #{cursorId}))
        </if>
        ORDER BY created_at DESC, post_id DESC
        LIMIT #{limit}
    </select>

</mapper>
