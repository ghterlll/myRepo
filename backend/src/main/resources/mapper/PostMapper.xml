<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mobile.aura.mapper.PostMapper">

    <!-- Result Map -->
    <resultMap id="BaseResultMap" type="com.mobile.aura.domain.content.Post">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="author_id" property="authorId"/>
        <result column="caption" property="caption"/>
        <result column="visibility" property="visibility"/>
        <result column="media_count" property="mediaCount"/>
        <result column="category" property="category"/>
        <result column="geo_lat" property="geoLat"/>
        <result column="geo_lon" property="geoLon"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted_at" property="deletedAt"/>
        <result column="status" property="status"/>
    </resultMap>

    <!-- List public posts with cursor pagination -->
    <select id="listPublic" resultMap="BaseResultMap">
        SELECT id, title, author_id, caption, visibility, media_count,
               created_at, updated_at, deleted_at, status
        FROM posts
        WHERE visibility = 'public'
          AND deleted_at IS NULL
        <if test="cursorTimestamp != null">
          AND (
            created_at &lt; #{cursorTimestamp}
            OR (created_at = #{cursorTimestamp} AND id &lt; #{cursorId})
          )
        </if>
        ORDER BY created_at DESC, id DESC
        LIMIT #{limit}
    </select>

    <!-- List posts from followed users with cursor pagination -->
    <select id="listFollowFeed" resultMap="BaseResultMap">
        SELECT id, title, author_id, caption, visibility, media_count,
               created_at, updated_at, deleted_at, status
        FROM posts
        WHERE author_id IN
        <foreach collection="followeeIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND visibility = 'public'
          AND deleted_at IS NULL
        <if test="cursorTimestamp != null">
          AND (
            created_at &lt; #{cursorTimestamp}
            OR (created_at = #{cursorTimestamp} AND id &lt; #{cursorId})
          )
        </if>
        ORDER BY created_at DESC, id DESC
        LIMIT #{limit}
    </select>

    <!-- Search public posts by keyword with cursor pagination -->
    <select id="searchPublic" resultMap="BaseResultMap">
        SELECT id, title, author_id, caption, visibility, media_count,
               category, geo_lat, geo_lon,
               created_at, updated_at, deleted_at, status
        FROM posts
        WHERE visibility = 'public'
          AND deleted_at IS NULL
          AND (
            title LIKE CONCAT('%', #{keyword}, '%')
            OR caption LIKE CONCAT('%', #{keyword}, '%')
          )
        <if test="category != null and category != ''">
          AND category = #{category}
        </if>
        <if test="cursorTimestamp != null">
          AND (
            created_at &lt; #{cursorTimestamp}
            OR (created_at = #{cursorTimestamp} AND id &lt; #{cursorId})
          )
        </if>
        ORDER BY created_at DESC, id DESC
        LIMIT #{limit}
    </select>

</mapper>
