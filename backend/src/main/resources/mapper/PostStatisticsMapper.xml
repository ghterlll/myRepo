<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mobile.aura.mapper.PostStatisticsMapper">

    <!-- Result Map -->
    <resultMap id="BaseResultMap" type="com.mobile.aura.domain.content.PostStatistics">
        <id column="post_id" property="postId"/>
        <result column="like_count" property="likeCount"/>
        <result column="comment_count" property="commentCount"/>
        <result column="bookmark_count" property="bookmarkCount"/>
        <result column="heat_score" property="heatScore"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <!-- Increment or decrement like count -->
    <update id="incLikeCount">
        UPDATE post_statistics
        SET like_count = GREATEST(like_count + #{delta}, 0),
            updated_at = NOW()
        WHERE post_id = #{postId}
    </update>

    <!-- Increment or decrement comment count -->
    <update id="incCommentCount">
        UPDATE post_statistics
        SET comment_count = GREATEST(comment_count + #{delta}, 0),
            updated_at = NOW()
        WHERE post_id = #{postId}
    </update>

    <!-- Increment or decrement bookmark count -->
    <update id="incBookmarkCount">
        UPDATE post_statistics
        SET bookmark_count = GREATEST(bookmark_count + #{delta}, 0),
            updated_at = NOW()
        WHERE post_id = #{postId}
    </update>

</mapper>
