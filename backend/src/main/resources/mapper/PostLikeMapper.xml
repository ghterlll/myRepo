<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mobile.aura.mapper.PostLikeMapper">

    <!-- Result Map -->
    <resultMap id="BaseResultMap" type="com.mobile.aura.domain.content.PostLike">
        <result column="post_id" property="postId"/>
        <result column="user_id" property="userId"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <!-- Count likes for a user on a post -->
    <select id="countLike" resultType="long">
        SELECT COUNT(1) FROM post_like
        WHERE user_id = #{userId} AND post_id = #{postId}
    </select>

    <!-- Delete like by user and post -->
    <delete id="deleteLike">
        DELETE FROM post_like
        WHERE user_id = #{userId} AND post_id = #{postId}
    </delete>

</mapper>
